# this_file: pyproject.toml
[project]
name = "getgo-fonts-site"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["properdocs==1.6.7", "mkdocs-materialx==10.1.5", "pymdown-extensions>=10.14"]
[tool.uv]
package = false
